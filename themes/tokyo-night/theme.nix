{
  id = "tokyo-night";
  name = "Tokyo Night";
  wallpaper = "sunset-lake.png";

  colors = {
    # ── Surfaces (darkest → lightest) ────────────────────────────────────────
    crust = "#0f0f17"; # deepest background
    mantle = "#16161e"; # dark panels / statuslines
    base = "#1a1b26"; # main application background
    surface = "#1f2335"; # raised cards / panels
    overlay = "#292e42"; # hover / selection bg
    border = "#3b4261"; # dividers / separators

    # ── Text hierarchy ────────────────────────────────────────────────────────
    text = "#c0caf5"; # primary text
    subtext = "#a9b1d6"; # secondary text
    muted = "#565f89"; # comments / inactive labels
    faint = "#414868"; # barely-there decorations

    # ── Primary accent ────────────────────────────────────────────────────────
    accent = "#b48cff"; # primary UI accent

    # ── Hue palette ───────────────────────────────────────────────────────────
    red = "#f7768e";
    orange = "#ff9e64";
    yellow = "#e0af68";
    green = "#9ece6a";
    teal = "#73daca";
    cyan = "#7dcfff";
    blue = "#7aa2f7";
    purple = "#b48cff";

    # ── Terminal ANSI (normal) ────────────────────────────────────────────────
    black = "#15161e";
    white = "#a9b1d6";

    # ── Terminal ANSI (bright) ────────────────────────────────────────────────
    brightBlack = "#414868";
    brightRed = "#ff7a93";
    brightGreen = "#b9f27c";
    brightYellow = "#ff9e64";
    brightBlue = "#7da6ff";
    brightPurple = "#b48cff";
    brightCyan = "#0db9d7";
    brightWhite = "#c0caf5";
  };
}
