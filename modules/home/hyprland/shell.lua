return {
  launcher = @launcher@,
  process_list = @processList@,
  bar_toggle = @barToggle@,
  volume_up = @volumeUp@,
  volume_down = @volumeDown@,
  volume_mute = @volumeMute@,
  mic_mute = @micMute@,
  setup = function()
    @setup@
  end,
}
